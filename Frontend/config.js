// ── App Configuration ─────────────────────────────────────────────────────────
// Change BACKEND_URL if you deploy the Flask backend to a different host/port.
// For local development, leave as is.
const CONFIG = {
  BACKEND_URL: 'https://workeval-backend.onrender.com'
};
